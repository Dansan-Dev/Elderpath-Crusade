package io.github.forest_of_dreams.characters.sprites.checker_sprites;

import io.github.forest_of_dreams.characters.sprites.checker_sprites.__super__.CheckerSprite;

public class WarpMageSprite extends CheckerSprite {
    public WarpMageSprite(int x, int y, int width, int height, AlignmentColor alignmentColor) {
        super(x, y, width, height, "Warp Mage", alignmentColor);
    }
}
